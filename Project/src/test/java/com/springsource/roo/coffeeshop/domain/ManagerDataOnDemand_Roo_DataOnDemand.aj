// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.springsource.roo.coffeeshop.domain;

import com.springsource.roo.coffeeshop.domain.Manager;
import com.springsource.roo.coffeeshop.domain.ManagerDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;

privileged aspect ManagerDataOnDemand_Roo_DataOnDemand {
    
    declare @type: ManagerDataOnDemand: @Component;
    
    private Random ManagerDataOnDemand.rnd = new SecureRandom();
    
    private List<Manager> ManagerDataOnDemand.data;
    
    public Manager ManagerDataOnDemand.getNewTransientManager(int index) {
        Manager obj = new Manager();
        setManagerFirstName(obj, index);
        setManagerID(obj, index);
        setManagerLastName(obj, index);
        setManagerPhoneNumber(obj, index);
        return obj;
    }
    
    public void ManagerDataOnDemand.setManagerFirstName(Manager obj, int index) {
        String ManagerFirstName = "ManagerFirstName_" + index;
        obj.setManagerFirstName(ManagerFirstName);
    }
    
    public void ManagerDataOnDemand.setManagerID(Manager obj, int index) {
        String ManagerID = "ManagerID_" + index;
        obj.setManagerID(ManagerID);
    }
    
    public void ManagerDataOnDemand.setManagerLastName(Manager obj, int index) {
        String ManagerLastName = "ManagerLastName_" + index;
        obj.setManagerLastName(ManagerLastName);
    }
    
    public void ManagerDataOnDemand.setManagerPhoneNumber(Manager obj, int index) {
        String ManagerPhoneNumber = "ManagerPhoneNumber_" + index;
        obj.setManagerPhoneNumber(ManagerPhoneNumber);
    }
    
    public Manager ManagerDataOnDemand.getSpecificManager(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        Manager obj = data.get(index);
        Long id = obj.getId();
        return Manager.findManager(id);
    }
    
    public Manager ManagerDataOnDemand.getRandomManager() {
        init();
        Manager obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return Manager.findManager(id);
    }
    
    public boolean ManagerDataOnDemand.modifyManager(Manager obj) {
        return false;
    }
    
    public void ManagerDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = Manager.findManagerEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'Manager' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<Manager>();
        for (int i = 0; i < 10; i++) {
            Manager obj = getNewTransientManager(i);
            try {
                obj.persist();
            } catch (final ConstraintViolationException e) {
                final StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    final ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
                }
                throw new IllegalStateException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
